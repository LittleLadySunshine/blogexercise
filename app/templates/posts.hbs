<section class="sidebar">
  <h3>Posts</h3>
  <ul>
    {{#each post in model}}
      <li>{{#link-to 'post' post}}{{post.title}}
        {{/link-to}}<button {{action 'deletePost' post}}>Delete</button></li>
    {{/each}}
  </ul>
</section>

{{outlet}}
